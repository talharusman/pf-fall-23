#include<stdio.h>
#include<math.h>
#include<limits.h>

int main(int argc, char const *argv[])
{
	int x=__INT_MAX__;
        printf("%d",x);
        x=INT_MIN;
        printf("%d",x);
        

	return 0;
}